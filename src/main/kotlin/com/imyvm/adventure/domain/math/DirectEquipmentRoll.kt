package com.imyvm.adventure.domain.math

object DirectEquipmentRoll {
    fun roll(
        seed: Long,
        tierWeights: Map<String, Double>,
        scopeIndex: Double
    ): String = ""

    fun materialExchange(input: Map<String, Long>, rate: Double): Map<String, Long> = emptyMap()
}
