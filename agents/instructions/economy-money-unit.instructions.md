---
applyTo: "src/main/kotlin/**/*.kt"
---

# 经济金额单位规则

1. 货币字段 `Long` 的数值除以 100 是玩家可见数值，保留两位小数。
2. 上层公式按玩家可见数值书写，落到钱包桥接前自行 `(value * 100).toLong()` 转换。
3. 函数名与字段名不附加单位后缀。
4. 文档与玩家可见文本写玩家可见数值，不暴露底层 `Long`。
